# Canonical install surface

Files in this directory are the recommended maintained entry points for installation. Historical variants remain recoverable from `archive-vanilla-origins` and may also remain at the repository root during migration.

Status labels are recorded in `/scripts.json`. Canonical files must have at least one current host and may list legacy aliases separately.
